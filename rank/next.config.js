/** @type {import('next').NextConfig} */
const nextConfig = {
  // output: "export", // Outputs a Single-Page Application (SPA).
  // distDir: "./build", // Changed to use default .next directory for Vercel deployment
  reactStrictMode: false,
};

export default nextConfig;
